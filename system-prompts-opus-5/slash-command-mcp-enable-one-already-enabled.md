<!--
name: 'MCP enable one: already enabled'
description: >-
  /mcp enable result for a single server already enabled but disconnected;
  injected as <local-command-stdout>.
ccVersion: 2.1.206
variables:
  - SLASH_COMMAND_MCP_ENABLE_ONE_ALREADY_ENABLED_VAR_0
-->
"${SLASH_COMMAND_MCP_ENABLE_ONE_ALREADY_ENABLED_VAR_0}" is already enabled but not connected. Run \`/mcp reconnect ${SLASH_COMMAND_MCP_ENABLE_ONE_ALREADY_ENABLED_VAR_0}\` to retry.
