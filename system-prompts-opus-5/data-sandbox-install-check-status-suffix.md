<!--
name: 'Data: Sandbox install — check status suffix'
description: >-
  Suffix appended to the /sandbox install success message when sandboxing is
  enabled in settings but not active; reaches the model as
  <local-command-stdout> command output.
ccVersion: 2.1.214
variables:
  - DATA_SANDBOX_INSTALL_CHECK_STATUS_SUFFIX_VAR_0
-->
${DATA_SANDBOX_INSTALL_CHECK_STATUS_SUFFIX_VAR_0}. Run /sandbox to check the current status.
