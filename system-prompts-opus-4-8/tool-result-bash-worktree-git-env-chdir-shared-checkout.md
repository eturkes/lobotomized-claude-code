<!--
name: 'Tool Result: Worktree git guard — env chdir to shared checkout'
description: >-
  Refusal clause: env changes directory into the shared checkout before running
  git.
ccVersion: null
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_ENV_CHDIR_SHARED_CHECKOUT_VAR_0
-->
changes directory via env to the shared checkout (${TOOL_RESULT_BASH_WORKTREE_GIT_ENV_CHDIR_SHARED_CHECKOUT_VAR_0}) before running git
