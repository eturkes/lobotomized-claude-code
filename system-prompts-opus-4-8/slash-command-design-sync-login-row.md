<!--
name: /design sync/login routing row
description: >-
  /design prompt table row explaining sync/login are normally routed elsewhere
  and to tell the user if disabled.
ccVersion: 2.1.206
-->
| `sync` / `login` | Normally routed to `/design-sync` / `/design-login` before reaching this prompt; seeing them here means that surface is disabled in this session — tell the user so. |
