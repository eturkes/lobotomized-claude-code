<!--
name: Artifact write page to file first
description: Artifact validation-steer guidance returned to the model.
ccVersion: 2.1.206
-->
Write the page to an .html or .md file first (Write/Edit), then call Artifact with `file_path` pointing at it. Set the title via an HTML `<title>` tag in the file.
