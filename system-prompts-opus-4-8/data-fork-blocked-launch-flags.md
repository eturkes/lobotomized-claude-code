<!--
name: 'Data: Fork blocked — launch flags'
description: >-
  /fork result when the session's launch flags (safe/bare mode, custom system
  prompt, tool allowlist, restricted settings) would not be inherited by the
  copy.
ccVersion: null
-->
Can't fork: this session was started with launch flags (safe or bare mode, a custom system prompt, a tool allowlist, or restricted settings) that the copy wouldn't inherit, so it would run with fewer restrictions than this session. Run the task here, or start a session without those flags and fork from there.
