<!--
name: 'MCP enable-all: already enabled but not connected'
description: >-
  /mcp enable-all result when servers are enabled but not connected; injected as
  <local-command-stdout>.
ccVersion: 2.1.206
variables:
  - SLASH_COMMAND_MCP_ENABLE_ALL_ALREADY_CONNECTED_VAR_0
-->
All MCP servers are already enabled, but ${SLASH_COMMAND_MCP_ENABLE_ALL_ALREADY_CONNECTED_VAR_0} ${SLASH_COMMAND_MCP_ENABLE_ALL_ALREADY_CONNECTED_VAR_0===1?"isn't":"aren't"} connected. Reply \`/mcp reconnect all\` here to retry.
