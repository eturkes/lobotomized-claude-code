<!--
name: Agent tool sync-only note
description: >-
  Conditional fragment of the Agent tool description telling the model
  background/name/mode are unavailable here.
ccVersion: 2.1.206
-->

- `run_in_background`, `name`, and `mode` are unavailable here — only synchronous subagents.
