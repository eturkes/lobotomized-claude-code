<!--
name: Artifact publish update instructions
description: >-
  Artifact publish tool_result block telling the model how to update/republish
  and where the user can browse artifacts.
ccVersion: 2.1.206
variables:
  - TOOL_RESULT_ARTIFACT_PUBLISH_UPDATE_INSTRUCTIONS_VAR_0
-->


To update: republish the same file path in this conversation (keeps this URL), or pass the URL as \`url\` from any other conversation — a conversation that didn't publish this artifact otherwise mints a new URL. Artifacts are private unless shared from the page's share menu; with Claude Code on the web, the user can browse theirs at ${TOOL_RESULT_ARTIFACT_PUBLISH_UPDATE_INSTRUCTIONS_VAR_0}.
