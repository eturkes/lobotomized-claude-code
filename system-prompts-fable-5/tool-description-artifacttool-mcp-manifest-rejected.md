<!--
name: 'Tool Result: MCP Manifest Rejected'
description: >-
  ArtifactTool tool_result noting the MCP manifest was rejected and the page
  published without the connector bridge.
ccVersion: 2.1.178
variables:
  - TOOL_DESCRIPTION_ARTIFACTTOOL_MCP_MANIFEST_REJECTED_VAR_0
-->
The MCP manifest was rejected by the server; the page was published without it, so its connector bridge is unavailable. Server said: ${TOOL_DESCRIPTION_ARTIFACTTOOL_MCP_MANIFEST_REJECTED_VAR_0.mcpDropped}
