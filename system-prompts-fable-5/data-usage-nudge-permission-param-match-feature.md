<!--
name: Deny/ask param rules feature
description: >-
  The feature string of the "permission-param-match" usage-nudge object,
  injected into model context as part of the usage-nudge catalog.
ccVersion: 2.1.206
-->
Deny and ask rules can match a tool input parameter — e.g., deny Agent(model:opus) or ask Bash(run_in_background:true) — so that specific pattern is auto-handled without prompting each time.
